# مراجعة المشروع وتدفق البيانات (my-metr)

## 1) نظرة عامة سريعة
المشروع عبارة عن تطبيق PHP (بدون Framework) لإدارة:
- حسابات المستخدمين (تسجيل/دخول/جلسات).
- رحلات العداد (بدء/تتبع/إنهاء/تحصيل).
- محفظة مالية (شحن/سحب/تحويل وتسجيل معاملات).
- شاشات إدارة للأدمن للمراجعة والموافقة.

المنظومة تعتمد على MySQL عبر `mysqli` في أغلب الملفات.

---

## 2) تدفق البيانات الحالي (Data Flow)

### A) المصادقة والهوية
1. المستخدم يسجل في `register.php`.
2. الباسورد يتخزن `password_hash`.
3. تسجيل الدخول في `login.php` يقرأ `users` ويتحقق `password_verify`.
4. يتم حفظ `user_id` و `role` في `$_SESSION`.

**نقطة مهمة:** صلاحيات الصفحات تعتمد غالبًا على قيمة `$_SESSION['role']` داخل كل صفحة.

### B) تدفق الرحلة
1. الواجهة في `ride.php` تجمع GPS وتعرض العداد الحي.
2. عند البدء، الواجهة تضرب `start_ride.php`:
   - يتحقق من CSRF والصلاحية.
   - يمنع وجود رحلة نشطة ثانية.
   - يقرأ الأسعار (`user_preferences` + `ride_settings`).
   - يخصم 1 جنيه من الرصيد، ويسجل معاملة `debit`.
   - ينشئ سجل رحلة في `rides` بحالة `started`.
3. أثناء الرحلة، `ride.php` يرسل دفعات تتبع (track_batch) وتليمترى.
4. عند الإنهاء، الواجهة تضرب `end_ride.php`:
   - يتحقق من ملكية الرحلة.
   - يحسب التكلفة النهائية.
   - يحول الحالة إلى `ended` ويخزن المسار.
5. التحصيل بعد الإنهاء:
   - كاش عبر `ride_pay_cash.php` (تأكيد دفع فقط + transaction).
   - تحويل لمستخدم عبر `ride_verify_recipient.php` + `ride_transfer_pay.php` (خصم/إضافة/تسجيل).

### C) تدفق الشحن (Recharge)
1. المستخدم يرفع طلب في `submit_recharge.php` (مبلغ + إثبات + رقم فودافون كاش).
2. يتم إنشاء `recharge_requests` بحالة `pending`.
3. الأدمن يراجع في `recharges.php` ثم:
   - موافقة `approve_recharge.php`: زيادة رصيد المستخدم + transaction + تحديث الطلب.
   - رفض `reject_recharge.php`: تحديث حالة الطلب فقط.

### D) تدفق السحب (Withdraw)
1. المستخدم يرسل طلب من `submit_withdraw.php`.
2. **حاليًا يتم خصم الرصيد فور الإرسال** ثم حفظ الطلب pending.
3. الأدمن يراجع في `withdraws.php`:
   - موافقة `approve_withdraw.php`.
   - رفض `reject_withdraw.php`.

### E) تتبع الحركة المالية
- `transactions.php`: عرض عمليات المستخدم + إحصائيات الداخل/الخارج.
- `all_transactions.php`: رؤية إدارية لكل العمليات.

---

## 3) الملاحظات الفنية الأهم

### (1) مخاطر أمنية عالية
- بيانات قاعدة البيانات (host/user/password) مكتوبة داخل الملفات (`config.php` و `includes/db.php`).
- وجود طريقتين اتصال مختلفتين وقاعدتي بيانات مختلفتين (`mysqli` و `PDO`) يزيد خطر عدم الاتساق.
- عدة عمليات إدارية تعتمد `GET` بدون CSRF (مثل approve/reject).

### (2) اتساق مالي غير مكتمل
- في السحب: يتم الخصم عند الطلب، ثم توجد خصومات إضافية عند الموافقة في مسارات معينة؛ هذا يفتح باب أخطاء محاسبية.
- أنواع `transactions.type` غير موحدة بالكامل بين الملفات (مثل `add`, `recharge`, `ride_deduction`, `debit`).

### (3) جودة معمارية
- منطق كبير جدًا داخل ملفات صفحات (مثل `ride.php` يجمع UI + API handlers).
- تكرار session/role checks في كل ملف بدل middleware مركزي.
- قلة طبقة خدمات/مستودعات لعمليات حرجة (الرحلات/المحفظة).

### (4) صحة البيانات
- الاعتماد أحيانًا على إدخال العميل في حساب تكلفة الرحلة (distance/duration القادمة من الواجهة) يحتاج تدقيق أقوى بالسيرفر.
- عمليات متعددة بدون تغليف Transaction موحد في كل المسارات المالية.

---

## 4) التحسينات المقترحة (مرتبة بالأولوية)

## أولوية 0 (سريع + عالي الأثر)
1. **نقل أسرار الاتصال لبيئة التشغيل** (`.env`) وإزالة أي كلمات مرور من الكود.
2. **توحيد الاتصال بقاعدة البيانات** (اختيار `mysqli` أو `PDO` فقط).
3. **فرض CSRF على كل عمليات التغيير** خصوصًا approve/reject عبر POST فقط.
4. **منع SQL المباشر مع متغيرات** واستبداله Prepared Statements بالكامل.
5. **توحيد enum لأنواع المعاملات** في مكان مركزي.

## أولوية 1 (صحة مالية)
1. توحيد Policy السحب:
   - إما خصم عند الإنشاء + ردّ عند الرفض.
   - أو الأفضل: حجز/تجميد (hold) ثم خصم نهائي عند الموافقة.
2. كل عملية مالية حرجة (خصم/إضافة/تحديث حالة/تسجيل transaction) تكون داخل Transaction واحدة.
3. إضافة `idempotency key` لواجهات الدفع حتى لا يتكرر الخصم مع إعادة الإرسال.

## أولوية 2 (معمارية وصيانة)
1. فصل API endpoints عن صفحات العرض (MVC بسيط أو folders: `api/`, `views/`, `services/`).
2. إنشاء طبقة Auth helpers (`requireLogin`, `requireRole`, `verifyCsrf`).
3. توحيد رسائل الأخطاء والاستجابة JSON.
4. تسجيل logs منظم (أخطاء/أحداث مالية) بدل `die()` المباشر.

## أولوية 3 (أداء ومراقبة)
1. فهارس (Indexes) للجداول الحرجة:
   - `rides(user_id, status, created_at)`
   - `transactions(user_id, created_at, type)`
   - `recharge_requests(status, created_at)`
   - `withdraw_requests(status, created_at)`
2. إضافة health checks وتقارير reconciliation يومية (رصيد المستخدمين مقابل دفتر المعاملات).

---

## 5) خارطة طريق تنفيذ مقترحة (3 مراحل)

### المرحلة 1 (1–2 يوم)
- إخفاء الأسرار + توحيد الاتصال + CSRF للعمليات الإدارية.
- مراجعة كل SQL ديناميكي وتحويله Prepared.

### المرحلة 2 (3–5 أيام)
- إعادة تصميم تدفق السحب المالي + idempotency + توحيد transaction types.
- عزل منطق wallet/ride في خدمات مستقلة.

### المرحلة 3 (أسبوع)
- هيكلة مشروع أوضح + logging + monitoring + اختبارات تكامل للمدفوعات.

---

## 6) خلاصة تنفيذية
المشروع ينجز الوظائف الأساسية بشكل جيد، لكن يحتاج **تقوية أمنية** و**توحيد مالي/معماري** قبل التوسع.
أهم 3 خطوات فورية: 
1) إلغاء الأسرار من الكود،
2) توحيد قاعدة/طبقة الاتصال،
3) ضبط دورة السحب والدفع داخل معاملات ذرّية آمنة وقابلة للتكرار بدون ازدواج.
